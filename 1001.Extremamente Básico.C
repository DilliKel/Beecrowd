#include <stdio.h>

int main() {
    int A, B, X;
    
    // Leitura dos valores inteiros A e B
    scanf("%d", &A);
    scanf("%d", &B);
    
    // Soma de A e B
    X = A + B;
    
    // Imprimir o resultado conforme o formato solicitado
    printf("X = %d\n", X);
    
    return 0;
}
